import generateLayout from "../lib/layout.js";
import { StyleSheet, Text, View } from 'react-native';

const Screen = () => {
  const layout = generateLayout();
  for(const row of layout){
    
  }

  return(
    <View>
        
    </View>
  )
}

export default Screen;